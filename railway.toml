[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start:postgresql-real"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[environments.production]
variables = { NODE_ENV = "production", PORT = "3000" }

[[services]]
name = "linisco-postgresql"
source = "."

[services.database]
type = "postgresql"
version = "15"